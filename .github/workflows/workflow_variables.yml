name: Testing Workflow Variable

on:
  workflow_dispatch

env:
  cloud: google-cloud

jobs:
  sample_job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo cloud variable
        run: echo "The cloud variable is $cloud"
